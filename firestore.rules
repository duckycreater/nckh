rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All writes come from the trusted server-side Firebase Admin SDK.
    // Client-side security rules are permissive since the app doesn't use
    // Firebase client SDK — all data flows through our Express backend.
    allow read, write: if true;
  }
}

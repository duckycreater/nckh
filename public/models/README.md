# Place ONNX models here after running:
#   cd training && pip install -r requirements.txt
#   python train_yolov8.py --mode all --epochs 15

# Expected files:
#   waste_classifier_v1.onnx     (~5 MB, MobileNetV3-Small backbone)
#   waste_classifier_v1_meta.json (class labels + accuracy metrics)
#